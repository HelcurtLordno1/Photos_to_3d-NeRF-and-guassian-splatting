# Processed data

`custom/<scene>/` chứa output của `ns-process-data`: ảnh đã resize/undistort,
`transforms.json` và sparse geometry dùng chung cho Nerfacto/Splatfacto.

Không chỉnh tay camera pose. Nếu protocol thay đổi, tạo scene/version mới để giữ
provenance.
